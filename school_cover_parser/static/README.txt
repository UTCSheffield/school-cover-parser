This directory contains static assets (floor plan images, SVGs, etc.)
that are referenced by the generated HTML output.

When the package is installed, these files are included as package data
so that the relative image URLs used in the HTML continue to work.
